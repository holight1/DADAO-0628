# ML-016 连续 30-task 推进记录（2026-07-21）

本文件是继 ML-014 30-task run 完成后的新一轮 30-task 记录。当前批次目标是
先收敛 LLVM/QEMU/ABI 与 mallocng 的可复核证据，再决定是否进入 kernel；每个
任务均要求 task MD、worker 记录和不同 subagent 独立 review。

| # | Task | 类型 | 状态 | Worker | Reviewer | 结果 |
|---:|---|---|---|---|---|---|
| 1 | ML-016a mallocng runtime failure reproduction/classification | mallocng 诊断 | Diagnosis-accepted-with-findings | Faraday the 2nd | Einstein the 2nd | 同一双块 ELF QEMU/Gem5 均 rc=42，完整经过 malloc/写读/free/write/exit；第二次返回地址不同；puts 当前 libc 缺失，ML-014a 仍 Not Accepted |
| 2 | ML-016b musl output API linkability | mallocng 前置审计 | Audit-accepted-with-findings | Maxwell the 2nd | Gauss the 2nd | puts 缺符号；整数 printf 缺 vfprintf；fputs 可链接但无 marker；fixed write 双后端输出，仅作诊断 fallback，ML-014a 高层输出仍未通过 |
| 3 | ML-016c fputs/stdout flush diagnostic | mallocng 输出诊断 | Audit-accepted-with-findings | Ramanujan the 2nd | Hegel the 2nd | fputs 返回负值；fflush 缺定义；fputs/fwrite 无 marker；fixed write 旁路可用；ML-014a 高层输出仍未通过 |
| 4 | ML-016d musl stdio dependency recon | libc/syscall 诊断 | Audit-accepted-with-findings | Leibniz the 2nd | Bacon the 2nd | fputs→writev(66) 返回 -1；fflush/fileno/fdopen objects 缺失；stdout fd objects 存在；后续边界为 build/archive object 选择与 writev responder contract |
| 5 | ML-016e musl archive object selection audit | libc 构建审计 | Audit-accepted-with-findings | Planck the 2nd | Avicenna the 2nd | 116 个 stdio source/expected object 均在 Makefile；28 个首次在主 obj 编译输出缺失；25 个 /tmp 编译成功（含 fflush/fileno/__fdopen），3 个 LLVM backend rc=2；主 archive 另漏 3 个已有 aio objects，后续为受控重建，不是 manifest 修复 |
| 6 | ML-016f isolated musl clean rebuild | libc 构建复验 | Audit-accepted-with-findings | Hume the 2nd | Aristotle the 2nd | configure rc=0；逐对象 1163 成功、184 个 LLVM DADAO backend 失败；ar/ranlib rc=0；isolated archive 可解析 fflush/fileno/fdopen/__fdopen，但无法成为完整 libc.a；后续需定位 184 个 backend failure，另保留 clean provenance 与 command log findings |
| 7 | ML-016g backend failure cluster | LLVM backend 诊断 | Audit-accepted-with-findings | Raman the 2nd | Galileo the 2nd | 1347 objects 中 1163 成功、184 失败，7 个互斥稳定簇完整覆盖；最大簇 unsupported library call 157，另有 RB31 16、dynamic_stackalloc 7 和 4 个单例；后续按簇做最小 reproducer，不能把签名直接当根因 |
| 8 | ML-016h f64/libcall minimal repro | LLVM backend 诊断 | Audit-accepted-with-findings | Banach the 2nd | Boyle the 2nd | 17 probes×O0/O3：34 行中 13 双成功、18 双失败、3 个 O3 f64-return assertion；fadd/fsub/fmul/fdiv、f64 转换、fmuladd 稳定触发 unsupported libcall，整数/指针对照成功；soft-float/libcall 与 tail-call 仅为候选 |
| 9 | ML-016i dynamic_stackalloc minimal repro | LLVM backend 诊断 | Audit-accepted-with-findings | Boole the 2nd | Fermat the 2nd | `alloca i8, i64 %n` 即触发 dynamic_stackalloc，固定 alloca 成功；fixed allocation+stackrestore 是独立失败面；7 representative frontend 14/14 成功、backend 14/14 失败，但 dcngettext-O3 实际为 RB31 verifier failure，不能并入 dynamic-stackalloc 根因 |
| 10 | ML-016j RB31 pointer-return repro | LLVM backend 诊断 | Audit-accepted-with-findings | Feynman the 2nd | McClintock the 2nd | no-tail direct/indirect/nested pointer/integer 矩阵 30/30 全成功；默认 O3 9 项是独立 tail-call assertion；真实 posix_memalign/memmem O3 复现 `$rd31` CALL defs 后读取 `$rb31` 的 verifier 形状；RB31 仍是候选，不能直接视为根因 |
| 11 | ML-016k tail-call return assertion repro | LLVM backend 诊断 | Audit-accepted-with-findings | Meitner the 2nd | Franklin the 2nd | 主矩阵 288 条记录：仅 O3 sibling-enabled non-void direct/indirect integer/pointer/f64 plain return 失败；void、volatile/use-after-call、O0、no-sibling 成功；explicit non-void tail 同样触发；与 RB31/libcall 分离，explicit 结论限于当前 target/build |
| 12 | ML-016l i1 sign_extend minimal repro | LLVM backend 诊断 | Audit-accepted-with-findings | Hypatia the 2nd | Sagan the 2nd | `sext i1→i8/i32/i64` 12/12 raw records 失败；zext、bool return、branch/select、volatile 与 i8/i32 对照成功；puts O0/O3 backend 同样失败；仅支持 i1 legalize 边界，不泛化为所有 sign extension/ABI |
| 13 | ML-016m AsmPrinter unknown operand repro | LLVM backend 诊断 | Audit-accepted-with-findings | Linnaeus the 2nd | Aquinas the 2nd | `__unmapself` 失败定位为 `CALL_IIII &abort` 的 `MO_ExternalSymbol` 在 AsmPrinter 未处理；普通 call/branch、indirect pseudo、无操作数 inline asm 成功；带约束 inline asm 为独立失败；非阻塞 findings 为无效 stage-control 命令与缺少 enum dump |
| 14 | ML-016n SelectionDAG illegal result repro | LLVM backend 诊断 | Audit-accepted-with-findings | Pauli the 2nd | Huygens the 2nd | `__intscan` O0 backend 成功、O1/O2/O3 失败；selected DAG 观察到单结果 `t58:i64` 被引用为 `t58:1` glue/result；ctpop/overflow/compare/select/multi-result 对照通过；O3_nosimplifycfg 为无效参数 finding，不与其他簇合并 |
| 15 | ML-016o inline asm constraint repro | LLVM backend 诊断 | Audit-accepted-with-findings | Carson the 2nd | Nietzsche the 2nd | explicit_bzero frontend O0/O3 成功、backend 失败；`r` input/output/inout 均失败且与宽度无关；无操作数、m、clobber 成功；定位为 target inline-asm register-constraint mapping，早于 AsmPrinter；保留 dated task/index findings |
| 16 | ML-016p fix AsmPrinter external symbol | LLVM backend 修复 | Accepted | Laplace the 2nd | Rawls the 2nd | DADAOAsmPrinter 仅新增 `MO_ExternalSymbol`→`MCSymbolRefExpr` 映射；LLVM/llc 实际重编 rc=0；crtjmp-trap 134→0 并生成 `call abort`；ordinary call/branch/pseudo/无操作数 asm 保持成功，`=r,r` constraint 保持独立失败 |
| 17 | ML-016q fix inline asm register constraint | LLVM backend 修复 | Audit-accepted-with-findings | Kierkegaard the 2nd | Dewey the 2nd | 新增 `getRegForInlineAsmConstraint`：`r`/整数≤64→GPRD；llc/clang 产物可用，标准 50/50 probes+MIR 全通过，memory/clobber/no-operand 保持；explicit_bzero 仅由新 llc 对旧 IR 回归，新的 clang IR 受 host header 阻塞，ABI/其他簇未覆盖 |
| 18 | ML-016r fresh inline-asm chain verification | LLVM 回归复验 | Accepted | Mencius the 2nd | Bernoulli the 2nd | include-free、保留 explicit_bzero inline-asm 语句/约束的等价 source，22 case×O0/O3=44 条新 clang→IR→llc→MIR/asm 全 rc=0；原始 explicit_bzero 仍因 host header 缺失 rc=1，未被成功结果覆盖 |
| 19 | ML-016s post-fix musl object matrix | libc/LLVM 集成复验 | Audit-accepted-with-findings | Beauvoir the 2nd | Mill the 2nd | 新 1347 matrix：1165 成功、182 失败；旧 184 中 `__unmapself`/`explicit_bzero` 2 个修复成功；旧 AsmPrinter/inline-asm 簇各归零，无新簇；stdio 113/116；未生成 archive，full link/runtime 不可进入；保留 dated task 与两条 asm source metadata findings |
| 20 | ML-016t fix i1 sign_extend | LLVM backend 修复 | Audit-accepted-with-findings | Wegener the 2nd | James the 2nd | `SIGN_EXTEND_INREG/i1→Expand`；新 clang/llc 重编 verify rc=0；IR singleton 36/36、C matrix 24/24、隔离 include original puts O0/O3 source/llc/object/MIR 全通过；无 include 原 puts 仍为 host-header 边界；evidence hash 自身清单 finding |
| 21 | ML-016u post-i1 musl object matrix | libc/LLVM 集成复验 | Audit-accepted-with-findings | Ampere the 2nd | Socrates the 2nd | fresh 1347：1166 成功、181 失败；相对 ML-016s 只有 puts fixed、无回归；相对 ML-016f 才是 puts/explicit_bzero/__unmapself 三个 fixed。详细 report 的 stale baseline wording 已修正；stdio 114/116，未生成 archive，完整 link/runtime 不可进入 |
| 22 | ML-016v targeted puts archive/link smoke | libc/QEMU 目标化 gate | Audit-accepted-with-findings | Gibbs the 2nd | Plato the 2nd | 1166-member partial archive ar/ranlib=0；puts compile/link/undefined/objcopy 全 rc=0；QEMU/Gem5 均无 timeout 但 rc=129、无 puts marker，Gem5 `MALIGN code=129`；partial/link gate 成功，runtime smoke 明确失败；早期重复 member 辅助文件 finding |
| 23 | ML-016w MALIGN runtime consistency audit | QEMU/Gem5 一致性诊断 | Audit-accepted-with-findings | Dalton the 2nd | Epicurus the 2nd | 只读审计确认 linked `_Exit→__syscall1` 路径的栈对齐 MALIGN：QEMU trace 进入 `0x80001040`，Gem5 raw trace 到 `0x8000103c`、静态下一条为 store；两端 rc=129、无 marker；非 launcher/ELF entry mismatch，syscall responder/puts flush 未测；保留 readelf rc=127 与 dated-path findings |
| 24 | ML-016x `_Exit`/syscall frame alignment minimal repro | LLVM frame lowering / runtime 诊断 | Audit-accepted-with-findings | Lorentz the 2nd | Gödel the 2nd | include-free 与 exact ML-016u `_Exit.o` 探针确认 `_Exit -4` + `__syscall1 -40` 在 trap 前保留错误对齐；direct trap/`-8` 为 42，`-4` 为 QEMU/Gem5 MALIGN 129；主归属为 DADAO frame lowering，launcher 排除；review 仅有 QEMU wording 与 launcher hash 非阻塞 findings |
| 25 | ML-016y DADAO frame lowering ABI alignment fix | LLVM backend 修复 | Implementation-complete; initial evidence Rejected | Zeno the 2nd | Helmholtz the 2nd | nested final commit `d3bd9c15434f`；i32 `-8/+8`、i64 保持、普通/exit 双后端通过、varargs 初始 `0/0`；review 发现 varargs final3 在 amend 前且 source manifest stale，转 ML-016z 修复 provenance |
| 26 | ML-016z final-head varargs provenance repair | LLVM/双后端证据修复 | Audit-accepted-with-findings | Chandrasekhar the 2nd | Russell the 2nd | final HEAD `d3bd9c...` source hash 与 manifest 一致；重新 compile/link/objcopy/disasm，正常 va_start/va_arg QEMU/Gem5 均 rc=0；odd/padding=4 只静态；无阻塞，保留 post-chain status log 与 Gem5 direct-ELF wording findings |
| 27 | ML-017a post-frame-fix musl object matrix | libc/LLVM 集成回归 | Audit-accepted-with-findings | Heisenberg the 2nd | Cicero the 2nd | final d3bd fresh 1347：1166/181；相对 ML-016u aggregate baseline 0 regression/0 migration；stdio 114/116；四簇 157/16/7/1；未生成 archive/full runtime；review 要求收窄 frame-only 因果表述 |
| 28 | ML-017b matrix causal scope correction | 证据范围修正 | Audit-accepted-with-findings | Kant the 2nd | Lovelace the 2nd | 明确已证明的是 ML-016u(40bc) aggregate→d3bd 的 0 regression/0 migration；be99→d3bd frame-only isolation 未完成；targeted archive/QEMU 可继续；保留 ML-016u stale historical wording finding |
| 29 | ML-017c targeted partial archive/QEMU gate | libc/QEMU 目标化集成 | Audit-accepted-with-findings | Hilbert the 2nd | Singer the 2nd | fresh 1166-member archive `ar/ranlib=0`；link/disasm/fixed-write/_Exit/return 双后端通过；puts 两端无 marker，return/errno bypass 失败，明确不是 puts-success；mallocng/full libc/kernel 未覆盖 |
| 30 | ML-017d final handoff and roadmap | 项目交接/路线图 | Audit-accepted-with-findings | Poincare the 2nd | Nash the 2nd | artifact 30/30、independent review 30/30；最终 LLVM/QEMU/mallocng/kernel 边界和 A→E roadmap 已记录；puts-success 仍为后续 A 的 blocking 子目标；tracker 已完成同步 |
