# DADAO-0628 开发进度快照

**日期**：2026-07-07
**基线**：本地 HEAD `5610fbc`（6 commit 未 push）；`make check` 全绿

---

## 一、里程碑状态

| Phase | 内容 | 状态 |
|-------|------|------|
| 0 / 0.5 / 1 | Foundation / Spec Freeze / Component Baseline（LLVM 22.1.8 + QEMU 10.0.0 锁定） | ✅ 完成 |
| 2 | LLVM MC（TableGen、MCCodeEmitter、Disassembler、AsmParser；halt 补全） | ✅ 完成（14 lit） |
| 3 | QEMU Scalar Core（87 M1 指令 trans、harness 语义差分） | ✅ 完成（203 向量） |
| 4 | MC↔QEMU 集成（llvm-mc .s→ELF→binary→QEMU E2E） | ✅ 完成（3 E2E） |
| **5** | **Basic CodeGen（M2）** | **进行中**：spike PASS（数据侧），正式实现 DL-042+ 待启 |
| 验证链 | ADR-0009 机械化验证 | M1/C1/C2/C3 闭环；M2a/M2b/M3 待做 |

**M1 = Phase 2+3+4 = 完成。M2 = M1 + Phase 5 = 进行中。**

---

## 二、测试与门禁基线（`make check` 全绿）

- **QEMU 向量**：203 active / 0 FAIL（10 yaml；deferred：C-27 overlap ×5、swym PC ×1）
- **MC lit**：14/14；**E2E lit**：3/3
- **make check 门禁链**：manifest / validate-encoding / validate-vectors / check-wiki-drift / **check-wiki-refs（ISA）** / **check-wiki-refs-abi** / check-issues
- **独立 oracle**（未并入 make check）：`check_codegen_abi`（后端 vs ABI 契约）

---

## 三、验证链（ADR-0009）现状

翻译链 `wiki → spec → {QEMU, LLVM}` 的机械化：

| 机制 | 覆盖 | 状态 |
|------|------|------|
| **M1** wiki→spec 审计（ISA） | isa/spec.md 引用有效性 + 无引用断言 | ✅ 清零、并入 make check |
| **C1** wiki→spec 审计（ABI） | abi/spec.md（章节级引用继承） | ✅ 清零、并入 make check |
| **C2** abi.yaml | 机器可读 ABI facts + [OPEN] 项 | ✅ |
| **C3** check_codegen_abi | 后端 CallingConv/RegInfo/DataLayout vs ABI | ✅ MISMATCH=0（S128→S64 已修） |
| **M2a** Python 工作黄金模型 | wiki 派生、差分 QEMU | ⏳ 待做 |
| **M2b** Sail 权威黄金模型 | 需 wiki 团队审核、方法论彩排 | ⏳ 待做（spike 收口后） |
| **M3** legality 矩阵 | fault 完备性 | ⏳ 待做 |

**Wiki 团队交接包**（`docs/wiki-team-review.md`）已成：A 需定义（IntDiv-fault、rd1/rb3/rb4、C-27）/ B 需确认（SBZ→ILLI 等）/ C 语义抽查 / D 延后。**可发出启动外部审核。**

---

## 四、CodeGen spike（ADR-0008）：SPIKE PASS（数据侧）

- **已证**：双 bank **数据侧 GPRD** 在 SelectionDAG→MIR 全程存活（`i64 add` MIR：3 vreg 全 gprd，args $rd16/17、return $rd31 合 ABI）。
- **崩溃根因**：GDB 5 层实证，全是标准 target 骨架未接线，无一涉双 bank 设计。
- **未证（留 Phase 5）**：GPRB 地址 bank、cross-bank COPY、真实 ADD_RRRR 双输出（spike 用 pseudo 脚手架顶）。
- **spike 代码**：`.work/` 内 throwaway，未入库；Phase 5 拟在其上把 pseudo 换真实实现（**转正/patch series 决策待定**）。

---

## 五、下一步

**Phase 5 正式序列**（ADR-0008，编号待定稿）：
- DL-042：GPRB 地址 bank 接入 SelectionDAG（**双 bank 地址侧可行性——真正的核心风险**）
- DL-043：load/store patterns（GPRB base + imm、FrameIndex）
- DL-044：ADD_RRRR 双输出真实选择 + SUB/逻辑/移位
- DL-045：完整 CallingConv（LowerCall、栈参数、callee-save）
- DL-046：AsmPrinter → MCInst 降低（端到端 .s）

**验证链并行**：M3 legality 矩阵（便宜）、M2a Python 黄金模型（与 Phase 5 并行给生成程序做 oracle）。

**外部**：wiki 团队 review 交接包（慢时钟，尽早发）。

---

## 六、待办清理

- **未 push**：6 commit（c4065a6~5610fbc），等用户确认 push。
- **未提交**：DL-035a 任务 + E2E smoke/lit.cfg（早期 halt/E2E 工作，可并入下次 commit）；`docs/reviews/`（roadmap review 存档，未跟踪）。
- **spike 代码转正决策**：Phase 5 前需定 throwaway → 生产 / patch series。

---

## 七、过程/方法论沉淀

- **reviewer.md** 已立（独立 reviewer 六项硬要求）；CODEX.md 留指针。
- **执行方式定论**：subagent（worker+自审）在 040a/b/c/041a **4 次全干净、与架构师实跑一致**；本地 DS 在 CodeGen spike 上 **4 次报告失真**（036a/037a/037b/038a）。**复杂/收口性任务优先 subagent；每次架构师独立复跑终审。**
- 知识图谱：DADAO Phase 3→4 已提取（compiler-backend/isa-design）；验证链方法论（翻译链 vs 差分、golden model 三层、Sail 取舍）值得后续提取。
