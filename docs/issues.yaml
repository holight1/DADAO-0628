# DADAO issue registry
# Status: open | closed
# Scope: M1 milestone or component domains
# Blocks: milestone gates or deliverables that depend on resolution
#
# Sources:
#   - docs/open-spec-issues.md
#   - contracts/isa/spec.md §Appendix C
#   - docs/wiki-questions.md

# ── Open issues (C-numbered) ──────────────────────────────────────────────────

- id: C-18
  title: "Hardware reset values (RD, RB[1-63], RF, RA power-on)"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: "ADR-0004 D2 (all reset values frozen for M1; full-system remains open)"

- id: C-27
  title: "csn/csz/csp/cseq/csne src=dst overlap snapshot rule"
  status: open
  scope: [M1]
  blocks: [overlap-vectors]
  resolved_by: null

- id: C-SBZ
  title: "SBZ fault semantics (ILLI vs UNDI for non-zero SBZ fields)"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: "ADR-0004 D5 (SBZ → ILLI exit 0x82)"

# ── Open issues (non-C-numbered, from open-spec-issues.md) ────────────────────

- id: TLB-fault-retry
  title: "TLB fault return: repair success skips instead of retrying faulting instruction"
  status: open
  scope: [System QEMU, Kernel]
  blocks: null
  resolved_by: null

- id: PTW-SBI-ABI
  title: "PTW PTE/PTHI/PAHI register-bank classification inconsistent with scalar ABI"
  status: open
  scope: [SBI, Kernel]
  blocks: null
  resolved_by: null

- id: VA2PA-result
  title: "VA2PA signed error encoding conflicts with full 64-bit physical addresses"
  status: open
  scope: [SBI, MMU tools]
  blocks: null
  resolved_by: null

- id: Varargs
  title: "Varargs save area, overflow area, aggregate values, and incoming-SP base layout"
  status: open
  scope: [ABI, libc]
  blocks: null
  resolved_by: null

- id: Cross-cfx-escape
  title: "Cross-cfx escape: previous cfx state and nested return policy"
  status: open
  scope: [Exception nesting]
  blocks: null
  resolved_by: null

- id: Multiple-returns
  title: "Multiple returns: mixed RD/RB/RF ordering ambiguity"
  status: open
  scope: [Advanced CodeGen]
  blocks: null
  resolved_by: null

# ── Closed items ──────────────────────────────────────────────────────────────

- id: C-14
  title: "rd2ra/ra2rd M1 inclusion (RA file move, not RAS)"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: architecture-decision-2026-06-29

- id: ADR-0003
  title: "ELF/object ABI (EM_DADAO=0x0DA0, e_flags=0x1 for M1, 10 relocation types)"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: ADR-0003

- id: ADR-0004
  title: "SBZ behavior (SBZ → ILLI exit 0x82; analogy to illegal operand in known opcode)"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: ADR-0004

- id: Wiki-13a414d
  title: "C-01–C-13, C-15–C-17, C-19–C-26 resolved by Wiki commit 13a414d (SimRISC 0.4.1)"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: wiki-13a414d

# ── Found by M1 wiki-ref audit (DL-039a/b/c) ─────────────────────────────────

- id: IntDiv-fault
  title: "整数 div-by-zero 与 divs INT64_MIN÷-1 → ILLI：spec §3.7 断言，但 wiki §乘除操作 未定义（DADAO-11-AEE 的 DZ 位仅为浮点状态）。当前记为 spec-decision，待 wiki 团队确认或补入 wiki。"
  status: open
  scope: [M1]
  blocks: null
  resolved_by: null

# ── Found by M2a golden model differential (DL-042a) ──────────────────────────

- id: ldo-align-MALIGN
  title: "ldo 非对齐 EA：spec §3.1 要求 8B 对齐→MALIGN，但 QEMU 不抛（exit0），且 rd-load-store.yaml case[2](+255)/case[6](+4095=-1) EA 非对齐却期望成功。黄金模型（从 spec）抛 MALIGN。→ 修 QEMU ldo 对齐检查 + 修 2 向量为对齐 EA 或 MALIGN 用例。QEMU+向量共同盲区，M2a 抓到。"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: DL-042b

- id: RASUF-cold-ret
  title: "冷 RegRAS（ra0 低48位=0）ret 单指令语义 = RASUF（spec §5.6），但 QEMU 落到 halt→ILLI，疑 RASUF 未实现。建议 M3 legality 矩阵覆盖 RASOF/RASUF。"
  status: open
  scope: [M1]
  blocks: null
  resolved_by: null

- id: opcodes-store-legality
  title: "opcodes.yaml 中 stb/stw/stt/sto/stm* 的 legality 为空，但 spec §2.6.1/§3.2 要求 store-from-rd0(rdha=rd0)→ILLI。编码契约不完备，建议回填。"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: DL-042b

# ── Found by M3 legality matrix (DL-043a) ─────────────────────────────────────

- id: QEMU-reserved-UNDI
  title: "保留编码：spec §2.5/§2.8.1 要求 UNDI(0x83)，但 QEMU 抛 ILLI(0x82)（5 例：MISC-Norm ha=0x01/0x0C/0x26、major op=0x11/0x18）。黄金模型 dadao_interp 对同编码抛 UNDI；全仓无 UNDI 向量。M3 抓到的 QEMU fault 完备性洞。→ 定夺：修 QEMU handler 分类 UNDI，或确认 M1 UNDI≡ILLI 取舍（若后者需 spec/golden/opcodes 同步）。"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: DL-043b

- id: opcodes-legality-cs-rb
  title: "opcodes.yaml 漏记 8 条 legality：cmp/csn/csz/csp 缺 rdhb!=rd0、cseq/csne 缺 rdhc!=rd0、RB add/sub 缺 rbhb!=rb0（spec §2.6.1/§2.6.2 明列，QEMU 均正确抛 ILLI）。stm* 同类洞新一批。→ 架构师直修补记（最小行改，勿 yaml.dump）。"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: architect-direct（DL-043a 后续）

# ── Found by M2a golden model full-87 differential (DL-042c) ──────────────────

- id: QEMU-rb0-not-maintained
  title: "QEMU 从不维护 env->rb[0]=PC+4（spec §1.3「rb0=current_PC+4，硬件维护」/§4.8 rela base=rb0[47:0]&~0xFFF）。全 dadao target 中 rb[0] 仅 translate.c:609 一处引用（读），无任何写 PC 的点，故 rb0 恒复位值≈0。rela（word 0x48040001）实际 PC≈BINARY_BASE=0x80000000 → 黄金模型 spec-correct 0x80001000；QEMU 读 rb0=0 得 0x1000，向量 rb-ops case[4] expected 也按 QEMU 写 0x1000（违 independent-oracle）。架构师初判「解释器 bug」经解码+QEMU 源码+探针三查证伪：是 QEMU 缺陷。→ 修 QEMU trans_rela 用 ctx PC+4（参 RISC-V auipc 用 ctx->base.pc_next）+ 同步修 rb-ops case[4] expected 0x1000→0x80001000（须同时落地）。影响面：任何以 rb0 为源的指令（rela / call 返回地址 / rb2rd rb0）在 QEMU 均读 0，Phase 5 CodeGen 用 rela 物化 PC-relative 地址前必须修。"
  status: open
  scope: [M1]
  blocks: [phase5-codegen-rela]
  resolved_by: null

- id: QEMU-rela-rbha-hi16-not-preserved
  title: "QEMU trans_rela 写目的 rbha 时用整值覆写（& 0x0000FFFFFFFFFFFF），清零了 rbha[63:48]，违反 spec §4.8 L785「rbha[63:48] = unchanged 保留」。预存 bug（DL-042d 前旧代码也整值覆写），DL-042d narrow 范围未触及。当前向量 rb-ops case[4] rb1 高位=0 故未暴露；黄金模型 dadao_interp 正确保留（(old & HI16)|res）。参照同文件 trans_addi_rb 的 read-modify-write 保留范式。→ 修 QEMU rela 保留高 16 + 加一条 rbha 高位非零的覆盖向量（M2a/M3 才能抓到分歧）。DL-042d review 架构师发现。"
  status: open
  scope: [M1]
  blocks: null
  resolved_by: null

- id: vector-shlu-rd0-mislabel
  title: "rd-shift-extend case[10] 向量缺陷：word 0x10443000 实解码为 shlu rd3,rd0,rd0（dest=rdhb=rd3≠rd0，合法），但 notes/class 标 dest=rd0→ILLI，编码与意图矛盾；且 rd0 混入 input_state（违 feedback_dadao_test_vector_constraints），harness setzw rd0 在 setup 即 ILLI（伪产物，根本没跑到 shlu）。M2a full-87 抓到。→ 架构师直修：word→0x10440040（真 dest=rd0）+ 去 input_state 的 rd0；QEMU+解释器均验证 ILLI。"
  status: closed
  scope: [M1]
  blocks: null
  resolved_by: architect-direct（DL-042c 收口）
