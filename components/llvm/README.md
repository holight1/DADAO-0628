# LLVM Component

M1 owns target registration, MC, and basic CodeGen. The patch series remains
empty until an ADR pins the upstream LLVM commit. Use same-version upstream
targets as API/style references; do not copy the legacy DADAO backend.
