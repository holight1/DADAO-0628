From fc6fa6835c5adf729d3d47d487c4d9d0d094fdfb Mon Sep 17 00:00:00 2001
From: suiyan <suiyan@sunmmio.com>
Date: Thu, 30 Jul 2026 17:36:53 +0800
Subject: [PATCH 03/11] Revert "dadao: widen SLUB cmpxchg result for K3 O0"

This reverts commit 8d49b7e041970743bb39e9dab94091a9036faeae.
---
 mm/slub.c | 9 +--------
 1 file changed, 1 insertion(+), 8 deletions(-)

diff --git a/mm/slub.c b/mm/slub.c
index f5cf5f142..1d3b35182 100644
--- a/mm/slub.c
+++ b/mm/slub.c
@@ -362,14 +362,7 @@ static __always_inline void slab_unlock(struct page *page)
 }
 
 /* Interrupts must be disabled (for the fallback code to work right) */
-#ifdef CONFIG_DADAO_K3_O0_LINK_COMPAT
-typedef unsigned long cmpxchg_double_slab_result_t;
-#else
-typedef bool cmpxchg_double_slab_result_t;
-#endif
-
-static inline cmpxchg_double_slab_result_t
-__cmpxchg_double_slab(struct kmem_cache *s, struct page *page,
+static inline bool __cmpxchg_double_slab(struct kmem_cache *s, struct page *page,
 		void *freelist_old, unsigned long counters_old,
 		void *freelist_new, unsigned long counters_new,
 		const char *n)
-- 
2.43.0

