From 73adec5c4c4391ff3b54f7accafc76abe38ef3ed Mon Sep 17 00:00:00 2001
From: suiyan <suiyan@sunmmio.com>
Date: Thu, 30 Jul 2026 17:36:54 +0800
Subject: [PATCH 09/11] Revert "dadao: widen compaction capture result for K3
 O0"

This reverts commit 8f0b11da8346dc46402974e7a6a8626cff103ed3.
---
 mm/page_alloc.c | 10 ++--------
 1 file changed, 2 insertions(+), 8 deletions(-)

diff --git a/mm/page_alloc.c b/mm/page_alloc.c
index 4248e4821..b060ceb52 100644
--- a/mm/page_alloc.c
+++ b/mm/page_alloc.c
@@ -826,12 +826,6 @@ static inline int page_is_buddy(struct page *page, struct page *buddy,
 	return 0;
 }
 
-#ifdef CONFIG_DADAO_K3_O0_LINK_COMPAT
-typedef unsigned long compaction_capture_result_t;
-#else
-typedef bool compaction_capture_result_t;
-#endif
-
 #ifdef CONFIG_COMPACTION
 static inline struct capture_control *task_capc(struct zone *zone)
 {
@@ -844,7 +838,7 @@ static inline struct capture_control *task_capc(struct zone *zone)
 		capc->cc->direct_compaction ? capc : NULL;
 }
 
-static inline compaction_capture_result_t
+static inline bool
 compaction_capture(struct capture_control *capc, struct page *page,
 		   int order, int migratetype)
 {
@@ -875,7 +869,7 @@ static inline struct capture_control *task_capc(struct zone *zone)
 	return NULL;
 }
 
-static inline compaction_capture_result_t
+static inline bool
 compaction_capture(struct capture_control *capc, struct page *page,
 		   int order, int migratetype)
 {
-- 
2.43.0

